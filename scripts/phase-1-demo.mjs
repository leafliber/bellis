#!/usr/bin/env node
/**
 * Phase 1 Demo 占位（phase-1-build-guide.md §15）。
 * 完整 Demo（提交 → 受控检查点终止 → 重启恢复）由 P4 交付；
 * 在此之前显式失败，避免占位输出伪装成验收成功。
 */
console.error("demo:phase1 尚未实现 — 该脚本由 P4（Runtime 集成、故障验证与 Demo）交付。");
console.error("参见 docs/phase-1-build-guide.md §13.5 与 §15。");
process.exit(1);
