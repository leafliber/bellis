# ADR 0044: the SDK is installed from the immutable archive in vendor/.
# Ordinary development dependencies use pnpm's default registry configuration.
# No local registry service or registry publication is required for this install.
