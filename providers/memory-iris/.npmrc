# ADR-0020 §11.2 stage one: the Core SDK is published to a local Verdaccio while
# the public npm scope is pending. Only @iris-memory is redirected; everything
# else resolves through the default registry.
@iris-memory:registry=http://localhost:4873/
